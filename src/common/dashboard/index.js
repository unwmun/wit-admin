import * as React from "react";
import { Card, CardContent, CardHeader } from "@material-ui/core";

const Dashboard = () => (
    <Card>
        <CardHeader title="Where is Treasure"/>
        <CardContent>Where is Teasure의 관리자 페이지</CardContent>
    </Card>
)

export { Dashboard };